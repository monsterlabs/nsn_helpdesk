page.replace_html 'filter_selected', :inline => Views::Helpers::Select.new()
#select('filter', 'filter_id', Region.all.collect {|p| [p.name, p.id]}, {:prompt => '-- Select --'})

# page.replace 'ticket_records',:inline => Views::Admin::Tickets::Record.new(:collection => Ticket.all.paginate(:page => params[:page] || 1, :per_page => params[:per_page] || 10).to_s(:helpers => self))