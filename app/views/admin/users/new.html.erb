<%= error_messages_for 'user'%>
<% form_for(:user, @user, :url => { :action => "create"}) do |f| %>

<h2>Add user</h2>

  <%= render :partial => 'person', :object => Person.new%>
  <%= render :partial => 'user', :locals => { :f => f}%>

<%= f.submit 'Create'%>
<% end%>
